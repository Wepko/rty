# js list 

### Проверка в js 
* Проверка на обьект
  * ``` typeof obj === 'object' && obj !== null ```
  
* Клонирование массива в js
  * ``` arr.slice()```
  * ``` [...arr] ```
  * ``` Array.from(arr) ```
  
